{% extends 'base.html.twig' %}

{% block title %}ReservationTaxi{% endblock %}

{% block body %}
    <h1>ReservationTaxi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_taxi.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reservation_taxi.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_taxi_index') }}">back to list</a>

    <a href="{{ path('app_reservation_taxi_edit', {'id': reservation_taxi.id}) }}">edit</a>

    {{ include('Admin/reservation_taxi/_delete_form.html.twig') }}
{% endblock %}
