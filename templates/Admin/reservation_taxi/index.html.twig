{% extends 'base.html.twig' %}

{% block title %}ReservationTaxi index{% endblock %}

{% block body %}
    <h1>ReservationTaxi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation_taxi in reservation_taxis %}
            <tr>
                <td>{{ reservation_taxi.id }}</td>
                <td>{{ reservation_taxi.status }}</td>
                <td>
                    <a href="{{ path('app_reservation_taxi_show', {'id': reservation_taxi.id}) }}">show</a>
                    <a href="{{ path('app_reservation_taxi_edit', {'id': reservation_taxi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reservation_taxi_new') }}">Create new</a>
{% endblock %}
